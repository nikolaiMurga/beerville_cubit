name: beerville_cubit
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc:
  get_it:
  injectable:
  equatable:

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  injectable_generator:

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - lib/resources/drawable/

  fonts:
    - family: mont
      fonts:
        - asset: lib/resources/fonts/mont.ttf

